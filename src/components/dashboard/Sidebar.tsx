/**
 * Dashboard sidebar. Placeholder for phase 1 — navigation, filters, and
 * collections are added in later phases.
 */
export function Sidebar() {
  return (
    <aside className="bg-sidebar text-sidebar-foreground hidden w-64 shrink-0 border-r p-4 md:block">
      <h2 className="text-lg font-semibold">Sidebar</h2>
    </aside>
  );
}
